services:
  qeloma-agent:
    build:
      context: .
    container_name: qeloma-agent
    env_file:
      - .env
    environment:
      PORT: 8000
    ports:
      - "8000:8000"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
